<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linkee.linkeeapi.report.query.mapper.ReportMapper">

    <!-- 전체 신고 목록 조회 (관리자) -->
    <select id="findAllReports" resultType="com.linkee.linkeeapi.report.query.dto.response.ReportListResponseDto">
        SELECT
            report_title,
            report_type,
            reporter_id,
            report_status
        FROM tb_report
        ORDER BY created_at DESC
            LIMIT #{offset}, #{limit}
    </select>

    <!-- 유저별 신고 조회 -->
    <select id="findReportsByUser" resultType="com.linkee.linkeeapi.report.query.dto.response.ReportListResponseDto">
        SELECT
            report_title,
            report_type,
            reporter_id,
            report_status
        FROM tb_report
        WHERE reporter_id = #{userId}
        ORDER BY created_at DESC
            LIMIT #{offset}, #{limit}
    </select>

    <!-- 관리자: 신고 상세 조회 -->
    <select id="findReportById" resultType="com.linkee.linkeeapi.report.query.dto.response.ReportDetailResponseDto">
        SELECT
            report_title,
            report_content,
            report_type,
            reporter_id,
            reported_id,
            report_action,
            report_status
        FROM tb_report
        WHERE report_id = #{reportId}
    </select>

    <!-- 일반 유저: 본인 신고만 상세 조회 -->
    <select id="findReportByIdAndReporter" resultType="com.linkee.linkeeapi.report.query.dto.response.ReportDetailResponseDto">
        SELECT
            report_title,
            report_content,
            report_type,
            reporter_id,
            reported_id,
            report_action,
            report_status
        FROM tb_report
        WHERE report_id = #{reportId}
          AND reporter_id = #{userId}
    </select>

</mapper>

