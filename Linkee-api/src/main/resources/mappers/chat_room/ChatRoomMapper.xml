<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linkee.linkeeapi.chat_room.query.mapper.ChatRoomMapper">
    <!-- 자신의 채팅방 목록 -->
    <select id="findMyChatRooms" resultType="com.linkee.linkeeapi.chat_room.query.dto.response.ChatRoomListResponseDto">
        SELECT
            cr.chat_room_name As chatRoomName,
            cr.room_owner AS ownerId,
            cr.joined_count As joinedCount
         FROM tb_chat_room cr
         JOIN tb_chat_member cm ON cr.chat_room_id = cm.chat_room_id
        WHERE cm.user_id = #{userId}
          AND cr.chat_room_type = 'CHAT'
          AND cr.room_status = 'Y'
        ORDER BY cr.created_at DESC
            LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countMyChatRooms" resultType="int">
        SELECT COUNT(*)
        FROM tb_chat_room cr
                 JOIN tb_chat_member cm ON cr.chat_room_id = cm.chat_room_id
        WHERE cm.user_id = #{userId}
          AND cr.chat_room_type = 'CHAT'
          AND cr.room_status = 'Y'
    </select>

    <!-- 전체 게임방 목록 -->
    <select id="findAllGameRooms" resultType="com.linkee.linkeeapi.chat_room.query.dto.response.GameRoomListResponseDto">
        SELECT
            cr.chat_room_name AS chatRoomName,
            cr.is_private AS isPrivate,
            cr.room_owner AS ownerId,
            cr.joined_count AS joinedCount,
            cr.room_capacity AS roomCapacity
        FROM tb_chat_room cr
        WHERE cr.chat_room_type = 'GAME'
          AND cr.room_status = 'Y'
        ORDER BY cr.created_at DESC
            LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAllGameRooms" resultType="int">
        SELECT COUNT(*)
        FROM tb_chat_room
        WHERE chat_room_type = 'GAME'
          AND room_status = 'Y'
    </select>
</mapper>